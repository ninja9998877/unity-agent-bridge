feat: compile 主动触发编译，并如实回报「是否真的编译了」

之前 compile 只「等」不「触发」：编辑器失焦时不会自动刷新资源，编译压根不会启动，
而 compilationFailed 反映的是**上一次**编译的状态 —— 于是会报出「编译通过 ✓」，
但驱动到的其实是旧程序集。这正是它本该防住的假象。

- BridgeActions.cs 新增 refresh：AssetDatabase.Refresh() 显式触发刷新/重编译，
  不依赖编辑器焦点
- bridge.py compile 先发 refresh 再等，并用两条独立证据判断「是否真的编译了」：
  ① isCompiling 翻转（权威，快轮询以免错过零点几秒的编译）
  ② 日志出现 script compilation（兜底，但 Unity 日志有缓冲，不能只靠它）
- compile 的返回值从 (ok, errors) 改为 {ok, triggered, compiled, errors}，
  调用方能分辨「编译通过」/「没编译发生」/「桥不支持 refresh」
- 修 _new_log_text：文本模式下用字节偏移 seek 会读到错误位置（假阴性），改二进制读

Co-Authored-By: Claude <noreply@anthropic.com>
